<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="windowActionModeOverlay">true</item>
    </style>

    <!-- inherits filterTouchesWhenObscured=true from parent -->
    <style name="RoundedButtonStyle" parent="@style/BaseBorderlessFilterButtonStyle">
        <item name="android:background">@drawable/content_selector</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- filter touches -->
    <style name="BaseBorderlessFilterButtonStyle" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:filterTouchesWhenObscured">true</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="EditTextHint" parent="AppTheme">
        <item name="colorAccent">@color/colorSecondaryText</item>
        <item name="android:textColorHint">@color/colorSecondaryText</item>
        <item name="colorControlNormal">@color/colorSecondaryText</item>
        <item name="colorControlActivated">@color/colorSecondaryText</item>
        <item name="colorControlHighlight">@color/colorSecondaryText</item>
        <item name="textColorError">@color/colorSecondaryText</item>
    </style>

    <style name="Checkbox" parent="AppTheme">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorHint">@color/colorPrimary</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="textColorError">@color/colorPrimary</item>
    </style>
</resources>
